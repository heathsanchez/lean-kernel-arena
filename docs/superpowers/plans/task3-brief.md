# Task 3: Nat literal conversion boundary

Working dir /workspace/scratch/feacdaa61cc5/kernel-work. No subagents, remote writes or PRs. Do not amend/rewrite shared git history. Change only semantic fix plus focused regression and report; parent owns CI and corpus runner.

Root-cause evidence reproduced on current production-equivalent stack: genesis/zero-sub-diagnostic.mjs runs public _build/tests/perf/grind-ring-5.ndjson at 2M steps. In Nat.zero_sub the final leaf comparison has a=application of projection(PProd,0,Nat.rec motive base step (nat 0)) to (nat 0), b=(nat 0), context [Nat]. The actual WHNF of BOTH sides is const Nat.zero. But this.normal(a) is const Nat.zero whereas this.normal(b) is nat 0 (cached result of native arithmetic). Existing nat-offset-defeq-layer.mjs only checks raw a,b BEFORE this normalization, then core equal sees const vs nat and returns rigid-head-mismatch. /tmp/zero-sub-normal.json contains exact sketches/results, raw expression diagnostic at genesis/zero-sub-diagnostic.mjs. This is a conversion scheduling/representation mismatch, not a missing subtraction theorem.

Implement minimum lawful repair. Prefer extending existing numeral/constructor comparison to appropriately exposed WHNF/normalized forms rather than adding name-specific Nat.zero_sub shortcut. Preserve Nat literal equivalence to zero/succ, refuse distinct natural numbers, do not turn arbitrary stuck terms into equal. Avoid unbounded unary expansion or decrementing huge unequal literals one at a time. Evaluate whether normalization cache invariants need correction versus exposing numeral comparison; justify choice and ablate it.

Test-first with a tiny real Kernel fixture from validated Nat export (e.g. _build/tests/tutorial/044_natDef.ndjson) plus lawful expression construction; show failed numeral-vs-computed-zero equality before patch, then succeeds; false pair 0 vs 1 and 2^64 vs different numeral must not succeed. Test normal cache warm/cold and operand orientation so the actual cached dual representation is exercised. Full exported giant diagnostic is available; use ulimit -s 65536 before node --stack-size=32768 (otherwise Linux 8MB process stack segfaults). Input env MATHGRAPH_INPUT_BYTE_LIMIT=20000000 MATHGRAPH_RECORD_LIMIT=400000. Parent concurrently runs full corpus; don't overwrite its output.

Run focused tests plus node genesis/test.mjs and one giant replay to identify new frontier. If a minimal fix does not eliminate the exact boundary, report diagnostics and don't stack speculative fixes. Report red/green, ablation, choices, remaining frontier in /workspace/scratch/feacdaa61cc5/task3-report.md. Commit only explicit semantic/test files. Return concise commit and result.
