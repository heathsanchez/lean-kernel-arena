description: |
  MathGraph materialized-key spine interner candidate.

  Reconstructs retained A6 from the frozen intgrah/sokonanoda pin, then applies
  the exact materialized-key authoritative spine representation that passed the
  frozen 103-good/58-bad semantic gate, four-case development gate, and
  disjoint 24-case held-out gate in heathsanchez/test.

url: https://github.com/heathsanchez/test
ref: novel-consequence-quotient-v1
rev: 3fb83f0fffb3e4d84a7ea970bdf8fe62cf5c762b

build: |
  git clone https://github.com/intgrah/sokonanoda.git upstream
  cd upstream
  git checkout 9b4ea12f4cd437d00b6bcd0e34743065c58dea08
  python3 ../scripts/patch_a6.py
  python3 ../scripts/patch_a6_materialized_key_spine.py
  cat > config.json <<__END__
   {
     "use_stdin": true,
     "nat_extension": true,
     "string_extension": true,
     "unpermitted_axiom_hard_error": false,
     "unsafe_permit_all_axioms": true,
     "num_threads": 4
   }
  __END__
  ROOT=$(cd ../../../../.. && pwd)
  "$ROOT/lka.py" build-test init-prelude
  RUSTFLAGS="-C target-cpu=native -Cprofile-generate=$PWD/pgo" cargo build --release
  target/release/sokonanoda config.json < "$ROOT/_build/tests/init-prelude.ndjson" >/dev/null
  llvm-profdata merge -o "$PWD/pgo/merged.profdata" "$PWD/pgo"
  RUSTFLAGS="-C target-cpu=native -Cprofile-use=$PWD/pgo/merged.profdata" cargo build --release

run: |
  upstream/target/release/sokonanoda upstream/config.json < "$IN" || exit 1
