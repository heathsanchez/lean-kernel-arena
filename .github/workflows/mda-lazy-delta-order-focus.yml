name: MDA lazy delta order focus
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/lazy-delta-order-focus.mjs'
      - '.github/workflows/mda-lazy-delta-order-focus.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  focus:
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 15s node genesis/test.mjs
      - name: Probe folded-definition-first lazy delta
        run: timeout 90s node experiments/lazy-delta-order-focus.mjs
