name: MDA iterative instantiation separator
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/iterative-instantiation.mjs'
      - '.github/workflows/mda-iterative-instantiation.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  separate:
    runs-on: ubuntu-24.04
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 15s node genesis/test.mjs
      - name: Test stack-safe declaration instantiation
        run: timeout 150s node experiments/iterative-instantiation.mjs
      - name: Preserve separator evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mda-iterative-instantiation-${{ github.sha }}
          path: genesis/evidence/iterative-instantiation-separator.json
          if-no-files-found: warn
