name: MDA pair beta WHNF composition
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/pair-beta-whnf.mjs'
      - '.github/workflows/mda-pair-beta-whnf.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  measure:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 20s node genesis/test.mjs
      - name: Compose external beta, structural WHNF and pair repair
        run: timeout 660s node experiments/pair-beta-whnf.mjs
      - name: Preserve evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pair-beta-whnf-${{ github.sha }}
          path: genesis/evidence/pair-beta-whnf.json
          if-no-files-found: warn
