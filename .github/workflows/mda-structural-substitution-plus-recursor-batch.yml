name: MDA structural substitution plus recursor batch
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'genesis/structural-substitution-plus-recursor-batch.mjs'
      - '.github/workflows/mda-structural-substitution-plus-recursor-batch.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  experiment:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Build focused witnesses
        shell: 'nix develop -c bash -euxo pipefail {0}'
        run: |
          ./lka.py build-test perf/shared-subterm
          ./lka.py build-test perf/folded-constant-first
          ./lka.py build-test perf/repeated-subproblem
          ./lka.py build-test perf/args-before-unfold
          ./lka.py build-test perf/discarded-argument-match
          ./lka.py build-test undecidability/alg-conv-trans-acc
          ./lka.py build-test undecidability/subject-reduction-reduct
      - name: Compose exact execution consequences
        run: timeout 150s node --stack-size=32768 genesis/structural-substitution-plus-recursor-batch.mjs
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: structural-substitution-plus-recursor-batch-${{ github.sha }}
          path: genesis/evidence/structural-substitution-plus-recursor-batch.json
          if-no-files-found: error
