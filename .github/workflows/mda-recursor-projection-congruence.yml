name: MDA recursor projection congruence
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/recursor-projection-congruence.mjs'
      - '.github/workflows/mda-recursor-projection-congruence.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  separator:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 20s node genesis/test.mjs
      - name: Run recursor projection congruence recovery
        run: timeout 420s node experiments/recursor-projection-congruence.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: recursor-projection-congruence-${{ github.sha }}
          path: genesis/evidence/recursor-projection-congruence.json
          if-no-files-found: error
          retention-days: 7
