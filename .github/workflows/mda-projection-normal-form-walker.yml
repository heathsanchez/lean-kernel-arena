name: MDA projection normal-form walker
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/projection-normal-form-walker.mjs'
      - '.github/workflows/mda-projection-normal-form-walker.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  replay:
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 15s node genesis/test.mjs
      - name: Replay single-pass projection normal-form congruence
        run: timeout 150s node experiments/projection-normal-form-walker.mjs
      - name: Preserve evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mda-projection-normal-form-walker-${{ github.sha }}
          path: genesis/evidence/projection-normal-form-walker.json
          if-no-files-found: warn
