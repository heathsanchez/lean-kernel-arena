name: MDA shared-subterm budget frontier
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'genesis/shared-subterm-budget-frontier.mjs'
      - '.github/workflows/mda-shared-subterm-budget-frontier.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Reconstruct retained present
        run: timeout 30s node --stack-size=32768 genesis/test.mjs
      - name: Build shared-subterm
        shell: 'nix develop -c bash -euxo pipefail {0}'
        run: ./lka.py build-test perf/shared-subterm
      - name: Trace budget frontier
        run: timeout 120s node --stack-size=32768 genesis/shared-subterm-budget-frontier.mjs
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: shared-subterm-budget-frontier-${{ github.sha }}
          path: genesis/evidence/shared-subterm-budget-frontier.json
          if-no-files-found: error
