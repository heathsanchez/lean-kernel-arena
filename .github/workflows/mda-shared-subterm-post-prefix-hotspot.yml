name: MDA shared-subterm post-prefix hotspot
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'genesis/shared-subterm-post-prefix-hotspot.mjs'
      - '.github/workflows/mda-shared-subterm-post-prefix-hotspot.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  profile:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
      - name: Build shared-subterm
        shell: 'nix develop -c bash -euxo pipefail {0}'
        run: ./lka.py build-test perf/shared-subterm
      - name: Profile residual after recursor-prefix reuse
        run: timeout 90s node --stack-size=32768 genesis/shared-subterm-post-prefix-hotspot.mjs
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: shared-subterm-post-prefix-hotspot-${{ github.sha }}
          path: genesis/evidence/shared-subterm-post-prefix-hotspot.json
          if-no-files-found: error
