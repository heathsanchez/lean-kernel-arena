name: MDA hot operation cache separator
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'genesis/hot-operation-cache-separator.mjs'
      - '.github/workflows/mda-hot-operation-cache-separator.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  separate:
    runs-on: ubuntu-24.04
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct frozen retained present
        run: timeout 15s node genesis/test.mjs
      - name: Separate exact hot-operation caches
        run: timeout 180s node --stack-size=32768 genesis/hot-operation-cache-separator.mjs
      - name: Preserve separator evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mda-hot-operation-cache-${{ github.sha }}
          path: genesis/evidence/hot-operation-cache-separator.json
          if-no-files-found: warn
