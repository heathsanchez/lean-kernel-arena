name: MDA post-sharing hotspot atlas
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'genesis/post-sharing-hotspots.mjs'
      - '.github/workflows/mda-post-sharing-hotspots.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  profile:
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct frozen retained present
        run: timeout 15s node genesis/test.mjs
      - name: Profile surviving residual after structural sharing
        run: timeout 120s node --stack-size=32768 genesis/post-sharing-hotspots.mjs
      - name: Preserve hotspot evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mda-post-sharing-hotspots-${{ github.sha }}
          path: genesis/evidence/post-sharing-hotspots.json
          if-no-files-found: warn
