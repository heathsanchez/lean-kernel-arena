name: MDA infer cache separator
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/infer-cache-separator.mjs'
      - '.github/workflows/mda-infer-cache-separator.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  separate:
    runs-on: ubuntu-24.04
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 15s node genesis/test.mjs
      - name: Test exact inference consequence reuse
        run: timeout 180s node --stack-size=32768 experiments/infer-cache-separator.mjs
      - name: Preserve separator evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mda-infer-cache-separator-${{ github.sha }}
          path: genesis/evidence/infer-cache-separator.json
          if-no-files-found: warn
