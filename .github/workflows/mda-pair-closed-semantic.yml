name: MDA pair closed semantic bank
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/pair-closed-semantic.mjs'
      - '.github/workflows/mda-pair-closed-semantic.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  separate:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 20s node genesis/test.mjs
      - name: Compile closed semantic consequences
        run: timeout 660s node experiments/pair-closed-semantic.mjs
      - name: Preserve evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pair-closed-semantic-${{ github.sha }}
          path: genesis/evidence/pair-closed-semantic.json
          if-no-files-found: warn
