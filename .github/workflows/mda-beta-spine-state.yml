name: MDA beta spine state
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/multi-beta-spine-state.mjs'
      - '.github/workflows/mda-beta-spine-state.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  separator:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 20s node genesis/test.mjs
      - name: Carry application spine as continuation state
        run: timeout 420s node experiments/multi-beta-spine-state.mjs
      - name: Preserve evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: beta-spine-state-${{ github.sha }}
          path: genesis/evidence/multi-beta-spine-state.json
          if-no-files-found: ignore
          retention-days: 7
