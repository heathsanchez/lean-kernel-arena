name: MDA prefix short-circuit substitution reuse
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'genesis/prefix-shortcircuit-substitution-reuse.mjs'
      - '.github/workflows/mda-prefix-shortcircuit-substitution-reuse.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  experiment:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
      - name: Build focused witnesses
        shell: 'nix develop -c bash -euxo pipefail {0}'
        run: |
          ./lka.py build-test perf/shared-subterm
          ./lka.py build-test perf/church-numerals
          ./lka.py build-test perf/args-before-unfold
          ./lka.py build-test perf/folded-constant-first
          ./lka.py build-test perf/repeated-subproblem
          ./lka.py build-test perf/discarded-argument-match
          ./lka.py build-test undecidability/alg-conv-trans-acc-left
          ./lka.py build-test undecidability/subject-reduction-redex
          ./lka.py build-test undecidability/alg-conv-trans-acc
          ./lka.py build-test undecidability/subject-reduction-reduct
      - name: Short-circuit binder occurrence before substitution
        run: timeout 150s node --stack-size=32768 genesis/prefix-shortcircuit-substitution-reuse.mjs
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: prefix-shortcircuit-substitution-reuse-${{ github.sha }}
          path: genesis/evidence/prefix-shortcircuit-substitution-reuse.json
          if-no-files-found: error
