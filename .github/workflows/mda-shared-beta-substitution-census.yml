name: MDA shared beta substitution census
on:
  push:
    branches:
      - mda-kernel-genesis-v1
    paths:
      - genesis/shared-head-beta-substitution-census.mjs
      - .github/workflows/mda-shared-beta-substitution-census.yml
  workflow_dispatch:
permissions:
  contents: read
jobs:
  census:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
      - name: Build shared witness
        shell: 'nix develop -c bash -euxo pipefail {0}'
        run: ./lka.py build-test perf/shared-subterm
      - name: Measure substitution programs
        run: timeout 150s node --stack-size=32768 genesis/shared-head-beta-substitution-census.mjs
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: shared-beta-substitution-census-evidence
          path: genesis/evidence/shared-head-beta-substitution-census.json
          if-no-files-found: error
