name: MDA unroll lazy trace
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'experiments/unroll-lazy-trace.mjs'
      - '.github/workflows/mda-unroll-lazy-trace.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  trace:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 15s node genesis/test.mjs
      - name: Trace unroll lazy delta
        run: timeout 60s node experiments/unroll-lazy-trace.mjs
