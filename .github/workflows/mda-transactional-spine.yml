name: MDA transactional spine separator
on:
  push:
    branches: [mda-kernel-genesis-v1]
    paths:
      - 'genesis/transactional-spine-separator.mjs'
      - '.github/workflows/mda-transactional-spine.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  separate:
    runs-on: ubuntu-24.04
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Reconstruct retained present
        run: timeout 15s node genesis/test.mjs
      - name: Test transactional spine conversion
        run: timeout 180s node --stack-size=32768 genesis/transactional-spine-separator.mjs
      - name: Preserve separator evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mda-transactional-spine-${{ github.sha }}
          path: genesis/evidence/transactional-spine-separator.json
          if-no-files-found: warn
