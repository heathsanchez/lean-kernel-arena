name: MDA post semantics giant budget threshold
on:
  push:
    branches:
      - mda-kernel-genesis-v1
    paths:
      - experiments/post-semantics-giant-budget-threshold.mjs
      - .github/workflows/mda-post-semantics-giant-budget-threshold.yml
  workflow_dispatch:
permissions:
  contents: read
jobs:
  threshold:
    runs-on: ubuntu-24.04
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v5
      - uses: cachix/install-nix-action@v31
      - name: Build giants
        shell: 'nix develop -c bash -euxo pipefail {0}'
        run: |
          ./lka.py build-test init-prelude
          ./lka.py build-test perf/grind-ring-5
      - name: Prepare nested parser candidate
        run: node experiments/make-zero-param-nested-parser-candidate.mjs
      - name: Add arbitrary precision Nat candidate
        run: node experiments/make-arbitrary-nat-candidate.mjs
      - name: Expose optParam constructor annotations
        run: node experiments/make-optparam-field-domain-candidate.mjs
      - name: Sweep execution budget
        run: timeout 360s node --stack-size=32768 experiments/post-semantics-giant-budget-threshold.mjs
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: post-semantics-giant-budget-threshold-evidence
          path: genesis/evidence/post-semantics-giant-budget-threshold.json
          if-no-files-found: error
